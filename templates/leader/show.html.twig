{% extends 'base.html.twig' %}

{% block title %}Leader{% endblock %}

{% block body %}
    <h1>Leader</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ leader.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ leader.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ leader.image }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ leader.country }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('leader_index') }}">back to list</a>

    <a href="{{ path('leader_edit', {'id': leader.id}) }}">edit</a>

    {{ include('leader/_delete_form.html.twig') }}
{% endblock %}
