{% extends 'base.html.twig' %}

{% block title %}Edit Leader{% endblock %}

{% block body %}
    <h1>Edit Leader</h1>

    {{ include('leader/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('leader_index') }}">back to list</a>

    {{ include('leader/_delete_form.html.twig') }}
{% endblock %}
