{% extends 'base.html.twig' %}

{% block title %}Civ6{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <img src="{{ asset('images/leaders/' ~ random_leader.image) }}" class="card-img-top" alt="{{ random_leader.name }}">
        </div>
    </div>
</div>
{% endblock %}
